根据数据情况猜解法、分治
面试中分治的应用场景：
1. 数据量整体做尝试可能性太多了，跑不完
2. 数据分成多个块（常见是两块）之后，各自的可能性并不算很多
3. 合并多个块各自信息的整合过程不复杂

子串：串中任意连续的子序列称之为子串
子序列：从某一初始位置开始，通过删除某些元素，但是不改变存留元素相对位置行程的序列，称为子序列；
分析一下时间复杂度：c++ 1s
                  java 2 - 4s 也即10的8次方如果时间复杂度为O(N^2)数据量不能超过10的6次方

题目一
给定一个非负数组arr,和一个正数M。返回arr的所有子序列中累加和%M之后的最大值